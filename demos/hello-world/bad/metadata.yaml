name: Hello World - Broken
description: |
  A broken version of the same deployment where the service is misconfigured.
source: https://kubernetes.io/docs/concepts/services-networking/service/
tags: [nginx, service, networking, basic]
objectives:
  - Observe why the service doesn't respond
  - Debug missing ports and invalid configurations
observations:
  - Pod runs fine, but curl fails
  - Logs may still show server running
  - Service selector may not match

