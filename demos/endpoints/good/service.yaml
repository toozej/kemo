---
apiVersion: v1
kind: Service
metadata:
  name: endpoints-demo
spec:
  # Intentionally no selector. Endpoints will be managed manually by run.sh.
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
